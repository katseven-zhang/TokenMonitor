# Codex reference migration coverage

Reference: local `codex-usage-desktop-main` v3.3.0. This is a coverage checklist, not a claim of complete parity. The user's pure-local requirement overrides reference features that need online accounts or services.

| Reference area | Desktop implementation | Work still required |
| --- | --- | --- |
| Dashboard hero and usage trends | Total/input/cache/output/reasoning/cost cards; minute ranges; dense empty buckets; bounded aggregation; local cost curve with unknown-price gaps, keyboard-accessible series toggles, expanded dialog and detailed tooltip; native expansion/toggle/Escape verified and populated light/dark charts inspected | OS DPI/text scaling acceptance remains separate |
| Daily and monthly tables | Both groupings, column sorting, pagination and range-intersecting drilldown; native daily and cross-month drilldown preserve original minute boundaries | Verified for the recorded native scenarios |
| Models and project analytics | Grouped totals and session drilldown; usage/cost shares, token composition, cache hit rate, effective price and last activity; model rows expand historical input/cache-read/cache-write/output effective prices; native category expansion and sorting passed; normalized Windows project native drilldown preserves minute range and canonical filter, with 866,626,437 tokens / 4,193 records matching its dashboard | Verified for the recorded native scenarios |
| Session usage and titles | Local titles, text search, session totals, timestamps, sortable columns and 25/50/100-row pagination; native sorting/paging and long-title wrapping verified; 951×651 navigation and 646-session/54k-event native rendering checked; observed 30-day RPC about 1.6s after activity payload fix | Evidence covers the recorded local dataset and window sizes, not arbitrary dataset sizes |
| Tool activity detail | Complete tool counts plus SQL-paged newest-first results; stable ties and Agent/minute/model/project/session/search isolation; native first/last navigation and range-change reset passed; 100k/1m indexed paging, Unicode search and concurrent writes verified | COUNT and OFFSET cost still grows with the matching range; see RELEASE-REVIEW-2026-09-20.md for measured scope |
| Conversation replay | Reference parser and modal adapted; native tool failures, raw disclosures/full JSONL and error/patch counts passed; real root plus six children expanded and switched; independent Decimal/cache family totals agree; fixed child-loading stale parent metrics and reverified natively | Existing reset/cumulative parser tests and six real replay/cache comparisons cover accounting; no unresolved defect from these recorded scenarios |
| Subscription limits | Local log observations with timestamps; half-open, archive-deduplicated history and window selector; disclosed 500-observation display cap; native table/filter/empty-state walkthrough passed; rendered reset-boundary tests retain historical balance and display waiting-for-new-log status at/after reset | No live account balance or actual provider rollover is inferred |
| Reset history and credits | Online retrieval/redemption deliberately excluded by user requirements; local history shows recorded reset timestamps and explicitly states timestamp changes do not prove manual resets or credit redemption | No unsupported manual reset/credit history is fabricated |
| Quota forecast | Reference `codex_limits.rs::fetch_codex_quota_forecast` performs an HTTP GET; excluded under the user's pure-local requirement. Local observed remaining percentage and recorded reset time remain available | No equivalent remote prediction is claimed; local logs do not establish future provider resets |
| Model catalog and prices | Offline JSON snapshot, editable rates, aliases and effective dates; searchable saved-price catalog with timestamp selection and historical entries; native valid/invalid save and errors verified; alias search, minute-effective price change and two-entry history expansion passed; temporary fixture removed with byte-identical original-file restoration | Verified for the recorded native scenarios |
| Settings and logs | Local data paths, theme, service port/control, autostart, price editor, logs; native port/restart/stop/cache and save/error verified; current-user startup registration checked/restored; hidden background launch and single-instance restoration passed; user confirmed tray operation normal | Actual login cycle is not performed during the user's active session |
| Updates, announcements, account login, remote assets | Excluded from runtime; production source request-API audit and 20 bounded process-tree TCP samples found no external connections | Bounded evidence does not constitute a permanent packet-level guarantee |

Cross-agent acceptance uses the same minute-range and pricing semantics for all ten sources. Unknown pricing remains unknown; it must not turn into a zero estimate. Detailed evidence and open validation items are tracked in `DESKTOP-VERIFICATION.md`.
