<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <UseWindowsForms>true</UseWindowsForms>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <AssemblyName>TokenMonitorTray</AssemblyName>
    <Version>1.4.3</Version>
    <Product>TokenMonitor</Product>
    <Description>TokenMonitor system tray capsule (loopback HTTP only, never reads the database)</Description>
    <!-- 自包含单文件 x64：发布命令见 windows/tray/build.ps1 -->
  </PropertyGroup>

</Project>
